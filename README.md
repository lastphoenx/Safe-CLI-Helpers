# Safe-CLI-Helpers
Two small but robust CLI helpers for Linux admins: fsx – safe file &amp; directory operations (dry-run by default). gix – safer everyday Git workflow (branches, push/pull, release) with confirmations.
